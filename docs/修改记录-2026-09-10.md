# 修改记录 — 2026-09-10

> 本次工作：完成下一轮迭代前的 P0 工程基线修复，目标是让项目能够在干净的 Windows 环境中安装、检查、启动并自动回归。

## 一、环境与启动

- Python 基准统一为 3.12，新增 `.python-version`。
- 新增 `environment.yml`，保留 Conda 安装方式。
- 新增 `requirements-dev.txt` 作为引擎、Web 后端、测试、下载器的统一依赖入口。
- 新增 `requirements-lock.txt`，锁定本次在 Python 3.12 上验证通过的完整依赖版本。
- 新增 `setup_windows.ps1`：默认创建项目 `.venv`，缺少 Python 3.12、FFmpeg、Node 时通过 winget 补齐，再安装 Python、前端、Remotion 与 Playwright 依赖。
- 新增 `scripts/preflight.py`：检查 Python/Node/FFmpeg、Python 包、两个 Node 工程与 Playwright Chromium。
- 重写 `start_web.bat`：移除旧电脑绝对路径，基于脚本目录启动；优先使用 `.venv`，兼容 `video-claw` Conda 环境和 `VIDEOCLAW_PYTHON`；启动前强制体检；支持 `--check`。
- `.gitignore` 增加 `.venv/`。

## 二、质量门禁

- 新增 `.github/workflows/ci.yml`：
  - Python 3.12 安装锁定依赖；
  - 引擎与下载器测试；
  - FastAPI 后端导入检查；
  - 前端 lint/build；
  - Remotion TypeScript 检查。
- 清理前端全部 lint 警告：修正 Hooks 依赖、拆分 Auth Context、移除未使用导入与混合导出。
- 修复 Remotion 5 个 TypeScript 错误：颜色插值参数、Easing.back 调用与无效 children 解构。
- 同步重新编译两个修复后的动态 Remotion 组件。

## 三、本机环境补齐

- Python 3.12.10
- FFmpeg 9.0.1
- Node.js 24.19.0 / npm 11.17.0
- 项目 `.venv` 完整依赖
- 前端与 Remotion `node_modules`
- Playwright Chromium 151

## 四、验证结果

| 验证项 | 结果 |
|---|---|
| `setup_windows.ps1 -SkipBrowser` 默认安装流程 | 通过 |
| `start_web.bat --check` | 通过 |
| 完整环境 `preflight.py --full` | 通过（仅提示尚未配置 API Key） |
| 引擎测试 | 54 passed |
| 下载器测试 | 3 passed |
| 前端 lint | 0 warning / 0 error |
| 前端生产构建 | 通过 |
| Remotion `tsc --noEmit` | 通过 |
| Python `pip check` | 通过 |
| Python 锁文件 dry-run | 通过 |
| Playwright Chromium 实际启动 | 通过 |
| FastAPI + Vite 临时启动健康检查 | 后端 `ok`，前端 HTTP 200 |
| npm audit（前端 / Remotion） | 0 vulnerability / 0 vulnerability |

## 五、说明

- AI 任务仍需用户在 Web 设置页或引擎 `.env` 中配置 API Key；这不会阻止 Web 平台启动。
- 本次只处理 P0 工程基线，没有改造 Gene 复用、Web Gene × Skill 生成闭环、密钥隔离与持久化任务队列；这些属于后续 P1/P2。
